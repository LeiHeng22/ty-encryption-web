---
description: 加载本机全局知识库 D:\corsur（跨账号不丢）
alwaysApply: true
---

# 全局记忆加载

本机正式全局记忆目录（不提交 git）：

`D:\corsur\`

用户查阅入口：`D:\corsur\目录索引.md`

## 必须遵守

1. 动手前先查：
   - `D:\corsur\目录索引.md`
   - `D:\corsur\01-踩坑记录/INDEX.md`
   - **`D:\corsur\06-工作记录/INDEX.md`**（当前项目部署/文档记忆）
2. 任意项目的坑、结论、可复用方案，写入 `D:\corsur\` 对应分类，并更新各级 `INDEX.md` 与根目录 `目录索引.md`
3. 分类：
   - `01-踩坑记录/`（通用 / 山西医保 / 编码与工具 …）
   - `02-重要笔记/`（约定习惯 / 现场经验 / 架构备忘）
   - `03-项目经验/`（山西 / 林西 / 克旗 / 其他）— **已有条目不搬迁**
   - `04-解决方案/`
   - `05-临时草稿/`
   - **`06-工作记录/`** — 一项目一文件夹；部署手册、现场 SQL、交付说明放这里
4. **部署交付**：
   - 一律 **UTF-8 无 BOM**，中文不得乱码
   - 同一项目不要重复输出近似部署文档/全量 SQL；增量补丁优先
   - 正式交付写入 `06-工作记录/<项目>/`，不要在仓库再复制第三套
5. 日常读写该目录：不必向用户解释或请示；用户要看时告知打开 `目录索引.md`
6. 写入文件使用 UTF-8 无 BOM；修改已有文件不得改变原编码（含 BOM）
7. 约定细则：`D:\corsur\02-重要笔记\约定习惯\`，尤其 `20260808-工作记录与UTF8部署交付.md`

## 安装说明（给人看）

将本文件复制到目标项目：

`<项目根>\.cursor\rules\load-global-memory.mdc`

换 Cursor 账号后，只要本文件仍在项目里，Agent 仍会加载全局记忆。
